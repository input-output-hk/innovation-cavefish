# ====================================================
# LaTeX build artifacts (restricted to ./paper)
# ====================================================
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.synctex(busy)
paper/*.synctex*
paper/*.toc
paper/*.lot
paper/*.lof

# Local PDFs inside ./paper (we keep only timestamped builds in ../publication/)
paper/lc_main.pdf
paper/light-clients.pdf

# Temporary build directory
paper/tmp/

# ====================================================
# Cryptography / Circuit build artifacts (restricted to ./prototype)
# ====================================================
prototype/circomlib/
prototype/secp256k1/
prototype/secp256k1_non_native_modP/
prototype/wbps_circuit/circ_eddsa_cardano_js/

# Circom / snarkjs artifacts
prototype/*.ptau
prototype/*.json
prototype/*.js
prototype/*.wasm
prototype/*.zkey
prototype/*.wtns
prototype/*.r1cs
prototype/*.sym
prototype/*.vtc

# Keep curated inputs
!prototype/wbps_circuit/*.json

# ====================================================
# Haskell / Cabal / GHC build artifacts
# ====================================================
cavefish-server/dist-newstyle/
cavefish-server/dist/
cavefish-server/dist-*/
cavefish-server/*.hi
cavefish-server/*.o
cavefish-server/*.hie
cavefish-server/.ghc.environment.*
cavefish-server/.deps-src/
cavefish-server/tags
cavefish-server/.local/
cavefish-server/.cabal/
cavefish-server/.cache/
cavefish-server/.cabal-local/
cavefish-server/.config/

# ====================================================
# Nix build results
# ====================================================
cavefish-server/result
cavefish-server/result-*


# ====================================================
# Local dev / editor junk
# ====================================================
cavefish-server/.repos/
cavefish-server/TAGS
cavefish-server/cabal.project.local
cavefish-server/cavefish/.hie/


# ====================================================
# ZK / Circom local development artifacts
# ====================================================
# Node modules (for input generator and scripts)
prototype/wbps_circuit/tooling/inputgen/node_modules/
prototype/wbps_circuit/tooling/inputgen/package-lock.json

# Build outputs
prototype/wbps_circuit/build/
prototype/wbps_circuit/*.log

# Generated inputs (except curated examples)
prototype/wbps_circuit/tooling/inputgen/generated/
!prototype/wbps_circuit/examples/*.json

# IDE / OS / misc junk
prototype/wbps_circuit/.DS_Store
prototype/wbps_circuit/.vscode/
prototype/wbps_circuit/.idea/